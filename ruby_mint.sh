#!/bin/bash
# Probably works in most debian based distributions
# install nvm
# install bundler
# add using rvm as a defult instead of defult system ruby bindings
# add following to ~./bashrc file so that it will work same on restart
source ~/.rvm/scripts/rvm
